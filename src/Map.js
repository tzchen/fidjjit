import React, { Component } from 'react';
import $ from 'jquery';

class Map extends Component {

  constructor(props) {
    super(props);
    this.state = {
      color: props.initialColor
    };
  }

  render() {
    return (
      
    );
  }
}

export default Map;
